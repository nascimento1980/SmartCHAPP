const express = require('express');
const router = express.Router();

// Placeholder para rotas de propostas
router.get('/', (req, res) => {
  res.json({ message: 'Proposals routes - em desenvolvimento' });
});

module.exports = router;